node_modules/
.env
.env*.local
.next/
.vercel/
pc/hls/
pc/.agent-hls/

# MediaMTX: a 30 MB binary, fetched by `npm run setup`, plus the config we
# generate from .env on every start (it contains the camera password).
pc/bin/

*.log
.DS_Store
Thumbs.db

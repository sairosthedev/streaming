opencv-python
numpy
ultralytics
flask
# Face recognition. dlib-bin is a prebuilt wheel so no C++ compiler is needed on
# Windows; plain `dlib` would try to compile and usually fails. If both are
# unavailable the detector still runs -- it just treats every pickup as a theft.
dlib-bin
face_recognition
face_recognition_models
# face_recognition_models still imports pkg_resources, which modern Python no
# longer ships by default. setuptools provides it. Without this you get a
# misleading "install face_recognition_models" error even when it IS installed.
setuptools<81
